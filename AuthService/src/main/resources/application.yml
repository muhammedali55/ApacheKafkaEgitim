server:
  port: 9080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/AuthDB
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update

